#main {
  margin-left: var(--aside-width);
  flex: 1;
  padding: var(--external-spacing);
}

@media (max-width: 690px) {
  #main {
    margin-left: 0;
    width: 100%;
  }
}

.main__container {
  bnn-width: 700px;
  bnn-position: center;
}
